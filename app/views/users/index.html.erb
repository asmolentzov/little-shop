<% @users.each do |user| %>
  <% if user.merchant? %>
    <ul>
      <li><%= user.name %></li>
      <li><%= user.city %></li>
      <li><%= user.state %></li>
      <li><%= user.created_at %></li>
    </ul>
  <% end %>
<% end %>
